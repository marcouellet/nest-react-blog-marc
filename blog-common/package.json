{
  "name": "blog-common",
  "version": "1.0.0",
  "description": "Contains common classes for backend and frontend",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marc Ouellet",
  "license": "ISC",
  "devDependencies": {
    "class-validator": "0.13.2",
    "typescript": "^4.0.0"
  }
}
